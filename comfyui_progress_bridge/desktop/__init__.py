"""Standalone native desktop monitor (install with the ``desktop`` extra)."""

from .settings import AppSettings, EndpointConfig, SettingsStore

__all__ = ["AppSettings", "EndpointConfig", "SettingsStore"]
