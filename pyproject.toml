[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "comfyui-progress-bridge"
description = "Mirror selected ComfyUI execution events to an external visual progress monitor over UDP."
version = "0.2.0"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Shenrui Ma" }]
dependencies = ["PyQt6>=6.7"]
keywords = ["comfyui", "progress", "monitoring", "custom-nodes", "udp"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
]

[project.urls]
Repository = "https://github.com/Shenrui-Ma/ComfyUI-Progress-Bridge"
Issues = "https://github.com/Shenrui-Ma/ComfyUI-Progress-Bridge/issues"

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.9"]

[project.scripts]
comfyui-progress-desktop = "comfyui_progress_bridge.desktop.app:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.setuptools.packages.find]
include = ["comfyui_progress_bridge*"]

[tool.setuptools.package-data]
comfyui_progress_bridge = ["web/*.js", "web/*.mjs"]
